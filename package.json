{
  "name": "fise-ai-platform",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy --dry-run",
    "deploy:staging": "wrangler deploy --config wrangler.staging.jsonc",
    "dev": "wrangler dev",
    "start": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.127.1"
  }
}
